package io.legado.app.model.local

import androidx.lifecycle.ViewModel

class ReadStatisticsViewModel : ViewModel() {
    // ViewModel implementation for read statistics
    // This can be expanded with additional functionality if needed
}